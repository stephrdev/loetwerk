build:
- dependencies
- install
- test
- testresults

install:
- cd spielwiese && python setup.py install

test:
- cd spielwiese && nosetests --with-xunit

options:
    unittest-xml-results: spielwiese/nosetests.xml
    pip-dependencies: spielwiese/requirements.txt